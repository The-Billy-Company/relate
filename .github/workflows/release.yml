# Publish the Python distribution to PyPI when a version tag is pushed.
#
# Deliberately much smaller than the `irregex` release. That package carries a
# compiled engine and needs a five-target wheel matrix; this one is pure Python
# and depends on `irregex` for every native byte it touches. So one
# `py3-none-any` wheel covers every platform, and — unlike that sibling — an
# sdist is genuinely buildable, because this project is self-contained under
# `bindings/python` rather than rooted above its own sources. The one thing it
# reaches up for is the repository README, which is the long description — so
# the sdist carries a corrected copy of it and a source build reads that.
#
# The distribution is `relate-search`; the import stays `relate`. The plain
# `relate` name on PyPI belongs to an unrelated author, so publishing under it
# is not available and installing under it would fetch a stranger's package.
# Naming them differently is the bs4 / PIL / cv2 pattern, and the same split
# `gist-search` made next door.
#
# What this does NOT publish: the `relate` binary. The Zig package is consumed
# through `build.zig.zon` (a tag's tarball, url+hash), and the CLI is built from
# source. Only the Python face has an index to publish to.
#
# ONE-TIME SETUP: this uses PyPI Trusted Publishing, so there is no API token to
# rotate or leak. On PyPI, add a trusted publisher for the `relate-search`
# project pointing at this repository, workflow `release.yml`, environment
# `pypi`.
name: release

on:
  push:
    tags: ["v*"]
  # Buildable and verifiable without publishing, so the artifact can be
  # exercised without minting a version.
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: bindings/python
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
        with:
          persist-credentials: false
      - uses: astral-sh/setup-uv@d4b2f3b6ecc6e67c4457f6d3e41ec42d3d0fcb86 # v5
        with:
          # Release artifacts must never be restored from a cache a pull
          # request could have populated.
          enable-cache: false

      # Every face starts at 1.0.0 together — that is the release where the
      # C ABI is frozen, so it would read badly for the thing being frozen to
      # carry a different number than the thing promising it. They diverge from
      # there: a binding's API churn has no business bumping the engine. So the
      # numbers being equal today is a starting line, not a coupling, and this
      # workflow publishes exactly one of them — the tag has to name that one's
      # version or the release notes describe an artifact nobody got.
      - name: The tag must name the version being published
        if: startsWith(github.ref, 'refs/tags/v')
        run: |
          set -euo pipefail
          tag="${GITHUB_REF_NAME#v}"
          declared=$(uv run --python 3.12 python -c \
            "import tomllib,pathlib;print(tomllib.loads(pathlib.Path('pyproject.toml').read_text())['project']['version'])")
          if [ "$tag" != "$declared" ]; then
            echo "::error::tag v$tag would publish relate-search $declared." \
                 "Tag the distribution's version, or bump pyproject.toml." >&2
            exit 1
          fi
          echo "tag v$tag matches the declared version"

      - name: Build sdist + wheel
        run: uv build --out-dir dist

      # A local checkout resolves the siblings through `[tool.uv.sources]`
      # paths, which is right for development — but that key is uv-specific and
      # never reaches core metadata, so what actually ships is whatever
      # `Requires-Dist` ended up saying. Assert on the built artifact rather
      # than on the source, because the source entry is supposed to be there and
      # a release cannot be taken back.
      - name: The published dependency must resolve from the index
        run: |
          set -euo pipefail
          uv run --python 3.12 python - <<'PY'
          import pathlib, re, sys, zipfile
          whl = next(pathlib.Path("dist").glob("*.whl"))
          zf = zipfile.ZipFile(whl)
          name = next(n for n in zf.namelist() if n.endswith(".dist-info/METADATA"))
          text = zf.read(name).decode()
          reqs = re.findall(r"^Requires-Dist:\s*(.+)$", text, re.M)
          print("Requires-Dist:", reqs or "(none)")
          if not any(r.split()[0].split("[")[0].strip() == "irregex" for r in reqs):
              sys.exit("the wheel does not depend on irregex — the substrate would be missing")
          bad = [r for r in reqs if "@" in r or "file://" in r]
          if bad:
              sys.exit(f"a direct/path reference would ship: {bad}")
          print("dependency metadata is index-resolvable")
          PY

      # The gate that earns its place: install the built artifact on the
      # declared floor, from the index for its dependencies, and prove it
      # imports. A `requires-python` that is a guess installs cleanly and then
      # fails at import, which is the worst possible place to find out — and
      # this package's floor is load-bearing rather than nominal, because
      # `corpus.py` uses PEP 695 `type` aliases that 3.11 cannot parse.
      - name: Prove the declared floor on the built artifact
        run: |
          set -euo pipefail
          uv venv --python 3.12 /tmp/floor
          uv pip install --python /tmp/floor/bin/python --find-links dist relate-search
          # From a directory that is not the project, so the import resolves to
          # the installed wheel rather than the working copy beside it.
          cd /tmp && /tmp/floor/bin/python -c "
          import relate
          print('floor import OK:', relate.__name__)
          "

      - name: Check the metadata the index will read
        run: uvx twine check dist/*

      - uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4
        with:
          name: dist
          path: bindings/python/dist/*
          if-no-files-found: error

  publish:
    # Only a tag publishes. A manual run builds and verifies and stops there.
    if: startsWith(github.ref, 'refs/tags/v')
    needs: build
    runs-on: ubuntu-latest
    environment: pypi
    permissions:
      # The OIDC token Trusted Publishing exchanges for an upload credential.
      id-token: write
    steps:
      - uses: actions/download-artifact@d3f86a106a0bac45b974a628896c90dbdf5c8093 # v4
        with:
          name: dist
          path: dist
      - uses: pypa/gh-action-pypi-publish@dc37677b2e1c63e2034f94d8a5b11f265b73ba33 # v1.14.2
  # ── crates.io
  #
  # A sibling of `publish`, not a step inside it. The two registries fail
  # independently, and a wheel that already uploaded must not be rolled back
  # because a crate upload failed — nor the reverse. Each gates on the same tag
  # and reads the same version, so they cannot disagree about what they publish.
  #
  # ONE-TIME SETUP: crates.io Trusted Publishing, so there is no registry token
  # to rotate or leak. On crates.io, add a trusted publisher to this crate
  # pointing at this repository, workflow `release.yml`, environment `crates-io`.
  # The first version had to be published by hand with an API token; crates.io
  # requires a crate to exist before it will accept a publisher configuration.
  crates:
    if: startsWith(github.ref, 'refs/tags/v')
    runs-on: ubuntu-latest
    environment: crates-io
    permissions:
      # The OIDC token crates.io exchanges for a short-lived registry token.
      id-token: write
    defaults:
      run:
        working-directory: bindings/rust
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
        with:
          persist-credentials: false

      # No toolchain action and no cache: `bindings/rust/rust-toolchain.toml`
      # pins the channel and the runner's rustup honours it, and a release
      # builds from source or not at all — a cache is writable by any workflow
      # on any branch, which is the wrong trade for an artifact being published.
      - name: The tag must name the version being published
        run: |
          set -euo pipefail
          tag="${GITHUB_REF_NAME#v}"
          declared=$(python3 -c \
            "import tomllib,pathlib;print(tomllib.loads(pathlib.Path('Cargo.toml').read_text())['package']['version'])")
          if [ "$tag" != "$declared" ]; then
            echo "::error::tag v$tag would publish crate version $declared." \
                 "Tag the version the crate declares, or bump Cargo.toml." >&2
            exit 1
          fi
          echo "tag v$tag matches the declared crate version"

      # crates.io renders a README as the entire project page, so the one it gets
      # is a link-corrected copy of the repository README rather than the short
      # one beside these sources. It is gitignored and minted here, immediately
      # before publishing: absent fails loudly, stale would ship quietly.
      - name: Mint the registry landing page
        working-directory: .
        run: python3 tools/registry_readme.py

      - uses: rust-lang/crates-io-auth-action@c6f97d42243bad5fab37ca0427f495c86d5b1a18 # v1.0.5
        id: auth

      # --locked: publish the dependency graph that was tested, not whatever the
      # index happens to offer at upload time.
      - run: cargo publish --locked
        env:
          CARGO_REGISTRY_TOKEN: ${{ steps.auth.outputs.token }}
  # ── Go module
  #
  # There is no registry to upload to: the proxy reads tags straight from the
  # repository. But a module in a subdirectory is resolved by a
  # subdirectory-prefixed tag, so `v1.2.3` publishes nothing for `bindings/go`
  # and `bindings/go/v1.2.3` publishes everything. The release that pushes the
  # first tag pushes the second, because a second tag typed by hand is the step
  # that gets forgotten.
  #
  # The App token is required rather than preferred: the enterprise blocks the
  # Actions identity from the writes this needs, and the same App already signs
  # the release PR.
  go-module:
    if: startsWith(github.ref, 'refs/tags/v')
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - id: app-token
        uses: actions/create-github-app-token@bcd2ba49218906704ab6c1aa796996da409d3eb1 # v3.2.0
        with:
          client-id: ${{ vars.RELEASE_APP_CLIENT_ID }}
          private-key: ${{ secrets.RELEASE_APP_PRIVATE_KEY }}
          permission-contents: write

      # No checkout. The tag is created through the API, so there is no clone to
      # make, no full history to fetch, and no credential written into a
      # `.git/config` that a later step could package up. The commit is
      # `github.sha`, which is the commit the version tag already names.
      - name: Tag the Go module at this commit
        env:
          GH_TOKEN: ${{ steps.app-token.outputs.token }}
          REPO: ${{ github.repository }}
          SHA: ${{ github.sha }}
          GO_TAG: bindings/go/${{ github.ref_name }}
        run: |
          set -euo pipefail
          # Idempotent: re-running a release must not fail on a tag it already
          # created, and must never move one that exists. A version the proxy has
          # served is immutable, so moving the tag would publish a lie.
          if existing=$(gh api "repos/$REPO/git/ref/tags/$GO_TAG" --jq '.object.sha' 2>/dev/null); then
            if [ "$existing" = "$SHA" ]; then
              echo "$GO_TAG already points at $SHA; nothing to do"
              exit 0
            fi
            echo "::error::$GO_TAG already exists at $existing, which is not this" \
                 "commit. A published Go version is immutable in the proxy; cut a" \
                 "new version rather than moving the tag." >&2
            exit 1
          fi
          # Annotated, in two calls: the object carries the message, the ref makes
          # it visible. A lightweight ref alone would lose the note entirely.
          obj=$(gh api "repos/$REPO/git/tags" -f tag="$GO_TAG" -f object="$SHA" \
                  -f type=commit -f "message=Go module $GITHUB_REF_NAME

          The module lives in a subdirectory, so the proxy resolves it by a
          subdirectory-prefixed tag rather than the repository's own version tag." \
                --jq '.sha')
          gh api "repos/$REPO/git/refs" -f ref="refs/tags/$GO_TAG" -f sha="$obj" --jq '.ref'
          echo "created $GO_TAG at $SHA"

      # The proxy ingests a version on first request, so asking for it here means
      # the first real `go get` is a cache hit rather than the request that pays
      # for ingestion — and a failure here is a loud signal that the tag did not
      # produce a usable module.
      - name: Prove the proxy can serve it
        env:
          MODULE: github.com/${{ github.repository }}/bindings/go
          VERSION: ${{ github.ref_name }}
        run: |
          set -euo pipefail
          for attempt in 1 2 3 4 5; do
            if GOFLAGS='' go list -m "$MODULE@$VERSION"; then
              echo "proxy serves $MODULE@$VERSION"
              exit 0
            fi
            echo "not visible yet (attempt $attempt); waiting"
            sleep 15
          done
          echo "::error::$MODULE@$VERSION never became resolvable through the" \
               "module proxy. The tag exists, so this is an ingestion problem." >&2
          exit 1
