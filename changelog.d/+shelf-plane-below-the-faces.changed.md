The codex shelf's lifecycle — its path, atomic write, fail-closed read, and staleness walk — moved down into `corpus/index/shelf/shelf.zig`, where all three faces stand. `relate quote`, `relate index --shelf`, and `irregex provenance` had been importing the `gist codex` verb module for it, which made a change to gist's verbs reach two other products; now each face only names its own rebuild command in the failure sentence, through the shared `outcome.needArtifact`.
