# relate tests

Behavioral kinship / retrieval / atlas suites, plus grade parity against
`src/kernel/kinship/metric/channel.zig`. Exact-search oracles use the `gist`
package as a **dev** dependency only.

```bash
cd bindings/python && uv run pytest
```
